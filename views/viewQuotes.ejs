<!DOCTYPE html>
<html>
<head>
  <title>View Quotes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container mt-5">
  <h2 class="mb-4">View Quotes</h2>
  <% if (!selectedName) { %>
    <h4>Select a Salesperson:</h4>
    <ul class="list-group">
      <% quotes.forEach(quote => { %>
        <li class="list-group-item">
          <a href="/viewQuotes?name=<%= encodeURIComponent(quote.name) %>">
            <%= quote.name %>
          </a>
        </li>
      <% }) %>
    </ul>
  <% } else { %>
    <h4>Quotes for <%= selectedName %></h4>
    <table class="table table-bordered table-striped">
      <thead class="table-dark">
        <tr>
          <th>Reference</th>
          <th>Customer Name</th>
          <th>Customer Email</th>
          <th>Customer Cell</th>
          <th>Job Description</th>
          <th>Progress</th>
        </tr>
      </thead>
      <tbody>
        <% quotes.forEach(quote => { %>
          <tr>
            <td><%= quote.reference %></td>
            <td><%= quote.customerName %></td>
            <td><%= quote.customerEmail %></td>
            <td><%= quote.customerCell %></td>
            <td><%= quote.jobDescription %></td>
            <td>...</td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } %>
  <a href="/viewQuotes" class="btn btn-secondary mb-2">Back to Salespeople</a>
  <a href="/login" class="btn btn-outline-primary mb-3 ms-2">← Back to Login</a>
</div>
</body>
</html>
